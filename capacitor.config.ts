import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ricksgarage.terminal',
  appName: "Rick's Garage Terminal",
  webDir: 'dist'
};

export default config;
